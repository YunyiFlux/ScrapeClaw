# -*- coding: utf-8 -*-
"""ScrapeEval-50 Standardized Benchmark Task Dataset."""
from typing import List
from scrapeclaw.benchmark.models import (
    BenchmarkTask,
    BenchmarkCategory,
    DifficultyLevel,
)

SCRAPEEVAL_50_TASKS: List[BenchmarkTask] = [
    # =========================================================================
    # Category 1: Pagination Models (10 tasks)
    # =========================================================================
    BenchmarkTask(
        id="PAGE-01",
        name="Standard Page-Step Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/pagination/page_step",
        goal="获取商品列表中的商品标题与价格，爬取前2页数据",
        expected_fields=["title", "price"],
        min_pass_items=6,
        description="Standard ?page=1&step=1 query parameter pagination",
    ),
    BenchmarkTask(
        id="PAGE-02",
        name="Offset-Limit Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/pagination/offset_limit",
        goal="提取前20条新闻的标题、发布时间与阅读数",
        expected_fields=["title", "date", "views"],
        min_pass_items=20,
        description="Standard ?offset=0&limit=10 offset based pagination",
    ),
    BenchmarkTask(
        id="PAGE-03",
        name="Cursor-based Token Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/pagination/cursor",
        goal="通过游标依次抓取前3页博文标题与作者",
        expected_fields=["title", "author"],
        min_pass_items=9,
        description="Next page query using cursor token parameter",
    ),
    BenchmarkTask(
        id="PAGE-04",
        name="Zero-Indexed Page Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/pagination/zero_index",
        goal="获取书籍列表前3页的书名与ISBN",
        expected_fields=["title", "isbn"],
        min_pass_items=12,
        description="Zero-indexed pagination starting at page=0",
    ),
    BenchmarkTask(
        id="PAGE-05",
        name="Custom Step Size Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/pagination/step_size_50",
        goal="抓取前2批商品（每批50件）的标题与库存",
        expected_fields=["title", "stock"],
        min_pass_items=50,
        description="Custom step size of 50 items per page",
    ),
    BenchmarkTask(
        id="PAGE-06",
        name="Empty Page Termination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/pagination/finite_pages",
        goal="抓取所有分类下的全部软件名称直到最后一页",
        expected_fields=["app_name", "downloads"],
        min_pass_items=15,
        description="Stops cleanly when the server returns empty items list",
    ),
    BenchmarkTask(
        id="PAGE-07",
        name="Timestamp-based Rolling Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/pagination/timestamp",
        goal="按最新时间戳向前滚动抓取近24小时评论",
        expected_fields=["comment_id", "content", "timestamp"],
        min_pass_items=15,
        description="Timestamp rolling boundary pagination",
    ),
    BenchmarkTask(
        id="PAGE-08",
        name="Descending Sorted Page Stream",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/pagination/sorted_desc",
        goal="获取热门榜单按点赞数降序排列的前2页视频标题与点赞数",
        expected_fields=["title", "likes"],
        min_pass_items=10,
        description="Pagination with sort=likes_desc contract",
    ),
    BenchmarkTask(
        id="PAGE-09",
        name="Infinite Scroll Virtual DOM",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/pagination/infinite_scroll",
        goal="通过滚动加载获取前3屏相册图片标题",
        expected_fields=["title", "image_url"],
        min_pass_items=12,
        description="Simulates virtual DOM infinite scroll load",
    ),
    BenchmarkTask(
        id="PAGE-10",
        name="Max Limit Clamped Pagination",
        category=BenchmarkCategory.PAGINATION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/pagination/clamped_limit",
        goal="提取分页接口被限制为单页最大5条的全部记录",
        expected_fields=["item_id", "name"],
        min_pass_items=10,
        description="Handles server-enforced small page limit clamping",
    ),

    # =========================================================================
    # Category 2: Interactive Navigation (8 tasks)
    # =========================================================================
    BenchmarkTask(
        id="NAV-01",
        name="Tab Switching to Tech Books",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/nav/tabs",
        goal="点击'科技数码'标签栏并提取其下所有书籍标题与作者",
        expected_fields=["title", "author", "category"],
        min_pass_items=3,
        description="Requires clicking category tab before capturing background API",
    ),
    BenchmarkTask(
        id="NAV-02",
        name="Keyword Search Input Trigger",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/nav/search",
        goal="在搜索框输入'Python'并回车，提取搜索结果列表标题与评分",
        expected_fields=["title", "rating"],
        min_pass_items=3,
        description="Requires inputting search query and pressing Enter",
    ),
    BenchmarkTask(
        id="NAV-03",
        name="Filter Toggle Selection",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/nav/filter_toggle",
        goal="勾选'仅显示自营现货'筛选器并抓取符合条件的商品名称",
        expected_fields=["title", "price", "is_in_stock"],
        min_pass_items=3,
        description="Clicking filter checkbox and capturing updated traffic",
    ),
    BenchmarkTask(
        id="NAV-04",
        name="Dropdown Menu Selection",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/nav/dropdown",
        goal="下拉选择'价格从低到高'排序并提取前5条商品信息",
        expected_fields=["title", "price"],
        min_pass_items=5,
        description="Selecting option from dropdown menu",
    ),
    BenchmarkTask(
        id="NAV-05",
        name="Button Click Load More",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/nav/load_more",
        goal="点击'加载更多'按钮2次，抓取全部追加展示的新闻标题",
        expected_fields=["title", "summary"],
        min_pass_items=10,
        description="Explicit 'Load More' button click interaction",
    ),
    BenchmarkTask(
        id="NAV-06",
        name="Nested Detail Modal Inspection",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/nav/detail_modal",
        goal="点击第一件商品的'查看详情'弹窗并提取详情描述与参数表",
        expected_fields=["title", "description", "specs"],
        min_pass_items=1,
        description="Clicking item card to trigger secondary detail API",
    ),
    BenchmarkTask(
        id="NAV-07",
        name="Multi-Tab Cross Contamination Guard",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/nav/purity_check",
        goal="严格只获取'游戏动漫'分区的视频，禁止混入'热门全部'分区视频",
        expected_fields=["title", "zone"],
        min_pass_items=3,
        description="Validates category purity and guard against general all-stream",
    ),
    BenchmarkTask(
        id="NAV-08",
        name="Breadcrumb Hierarchy Navigation",
        category=BenchmarkCategory.NAVIGATION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/nav/breadcrumbs",
        goal="通过面包屑导航进入'电子产品 > 笔记本电脑'并提取产品清单",
        expected_fields=["model", "cpu", "price"],
        min_pass_items=4,
        description="Multi-tier breadcrumb navigation interaction",
    ),

    # =========================================================================
    # Category 3: Data Hierarchy & Unpacking (8 tasks)
    # =========================================================================
    BenchmarkTask(
        id="UNPACK-01",
        name="Root List Direct Array",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/unpack/root_array",
        goal="抓取根节点直接为 JSON 数组的商品标题与单价",
        expected_fields=["title", "price"],
        min_pass_items=5,
        description="API response is a plain JSON array [ {...}, {...} ]",
    ),
    BenchmarkTask(
        id="UNPACK-02",
        name="Dot-Notation Data Items",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/unpack/data_items",
        goal="抓取 data.items 路径下的视频标题与播放量",
        expected_fields=["title", "view_count"],
        min_pass_items=5,
        description="Standard data.items wrapper response",
    ),
    BenchmarkTask(
        id="UNPACK-03",
        name="Deep Nested Results Payload",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/unpack/deep_payload",
        goal="抓取 response.body.payload.result.items 路径下的全部记录",
        expected_fields=["record_id", "name"],
        min_pass_items=4,
        description="Multi-layer dot notation object unpack",
    ),
    BenchmarkTask(
        id="UNPACK-04",
        name="GraphQL Edges-Node Unpacking",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/unpack/graphql_edges",
        goal="抓取 GraphQL edges[*].node 嵌套模型下的文章标题与作者",
        expected_fields=["title", "author"],
        min_pass_items=4,
        description="Extracts items from GraphQL edges[*].node structure",
    ),
    BenchmarkTask(
        id="UNPACK-05",
        name="Single Object User Profile",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/unpack/single_profile",
        goal="抓取单个用户个人资料中的用户名、UID与粉丝数",
        expected_fields=["username", "uid", "followers"],
        min_pass_items=1,
        description="Single dictionary profile response rather than list",
    ),
    BenchmarkTask(
        id="UNPACK-06",
        name="Keyed Object Map to List",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/unpack/keyed_map",
        goal="将以 ID 为键的对象字典展平提取为商品列表",
        expected_fields=["id", "name", "price"],
        min_pass_items=4,
        description="Unpacks dictionary with dynamic item IDs as keys",
    ),
    BenchmarkTask(
        id="UNPACK-07",
        name="Heterogeneous Mixed Types in Array",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/unpack/heterogeneous",
        goal="从包含广告Banner与视频卡片的混合数组中仅过滤提取视频信息",
        expected_fields=["title", "duration"],
        min_pass_items=4,
        description="Filters valid content items out of heterogeneous mixed card stream",
    ),
    BenchmarkTask(
        id="UNPACK-08",
        name="Nested Comments and Replies",
        category=BenchmarkCategory.UNPACKING,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/unpack/nested_replies",
        goal="提取一级评论内容并展开抓取其下所有二级回复的文本",
        expected_fields=["comment", "replies"],
        min_pass_items=3,
        description="Recursive tree or nested list unnesting",
    ),

    # =========================================================================
    # Category 4: Authentication & Session Lineage (6 tasks)
    # =========================================================================
    BenchmarkTask(
        id="AUTH-01",
        name="Cookie Injection Authentication",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/auth/cookie_echo",
        goal="携带 auth_token Cookie 凭据访问并抓取当前登录用户资产",
        expected_fields=["user_id", "assets", "role"],
        min_pass_items=1,
        description="Requires session cookies mounted to pass 401 guard",
    ),
    BenchmarkTask(
        id="AUTH-02",
        name="Bearer Token Authorization Header",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/auth/bearer_header",
        goal="携带 Authorization: Bearer 头抓取私密通知列表",
        expected_fields=["msg_id", "title", "content"],
        min_pass_items=3,
        description="Validates bearer token in HTTP headers",
    ),
    BenchmarkTask(
        id="AUTH-03",
        name="Dynamic Session File Override",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/auth/dynamic_override",
        goal="验证通过 --session 动态注入外部凭据覆盖默认登录状态",
        expected_fields=["active_account", "level"],
        min_pass_items=1,
        description="Validates synthesized crawler --session reload behavior",
    ),
    BenchmarkTask(
        id="AUTH-04",
        name="Playwright StorageState JSON Import",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/auth/storage_state",
        goal="从外部 storage_state.json 自动导入多域名 Cookies 并在探针中应用",
        expected_fields=["session_ok", "uid"],
        min_pass_items=1,
        description="Standard Playwright storage_state format parsing",
    ),
    BenchmarkTask(
        id="AUTH-05",
        name="Raw F12 Cookie String Import",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/auth/raw_cookie",
        goal="从直接复制的 Raw Cookie 文本字符串成功解析并挂载会话",
        expected_fields=["parsed_ok"],
        min_pass_items=1,
        description="Raw Cookie: a=1; b=2 string parsing",
    ),
    BenchmarkTask(
        id="AUTH-06",
        name="Session Persistence Export",
        category=BenchmarkCategory.AUTH_SESSION,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/auth/session_export",
        goal="探针完成抓取后通过 --save-session 持久化导出最新活跃会话",
        expected_fields=["exported"],
        min_pass_items=1,
        description="Exports captured browser context cookies upon completion",
    ),

    # =========================================================================
    # Category 5: Anti-Scraping Defense & Signatures (8 tasks)
    # =========================================================================
    BenchmarkTask(
        id="ANTI-01",
        name="Referer Anti-Hotlink 403 Guard",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/anti/referer_guard",
        goal="抓取需携带严格 Referer 来源头的商品列表",
        expected_fields=["title", "price"],
        min_pass_items=4,
        description="Returns 403 Forbidden unless correct Referer is present",
    ),
    BenchmarkTask(
        id="ANTI-02",
        name="Custom User-Agent Fingerprint Check",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/anti/ua_check",
        goal="验证爬虫请求头携带现代浏览器真实 User-Agent 通过检测",
        expected_fields=["status", "data"],
        min_pass_items=3,
        description="Blocks python-requests or httpx default user-agents",
    ),
    BenchmarkTask(
        id="ANTI-03",
        name="Dynamic MD5 Query Parameter Signature",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/anti/md5_signature",
        goal="自动嗅探 URL 携带的 sign=MD5 参数并在合成爬虫中保留签名",
        expected_fields=["item_id", "title"],
        min_pass_items=3,
        description="API requires sign parameter generated from query keys",
    ),
    BenchmarkTask(
        id="ANTI-04",
        name="Dynamic SHA256 WBI Signature",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/anti/sha256_wbi",
        goal="嗅探仿 Bilibili WBI 加密签名参数并提取视频列表",
        expected_fields=["bvid", "title"],
        min_pass_items=3,
        description="Simulates modern video portal WBI web signature",
    ),
    BenchmarkTask(
        id="ANTI-05",
        name="Rate Limit 429 Adaptive Backoff",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/anti/rate_limit",
        goal="在短时间触发 429 请求过多时自动指数退避重试并完成抓取",
        expected_fields=["job_id", "title"],
        min_pass_items=3,
        description="Returns HTTP 429 initially, passes on delayed retry",
    ),
    BenchmarkTask(
        id="ANTI-06",
        name="Noise Header Pruning Contract",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/anti/noise_headers",
        goal="自动剔除无关临时追踪头（Sec-Ch-Ua等），仅保留最小必需请求头",
        expected_fields=["content"],
        min_pass_items=2,
        description="Validates diff_probe pruning on noisy browser headers",
    ),
    BenchmarkTask(
        id="ANTI-07",
        name="HTTP POST JSON Payload Contract",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/anti/post_json",
        goal="以 POST JSON 请求体发送查询参数抓取分析数据",
        expected_fields=["query", "count"],
        min_pass_items=3,
        description="API accepts POST method with JSON payload instead of GET",
    ),
    BenchmarkTask(
        id="ANTI-08",
        name="Encoded Base64 Cursor Security",
        category=BenchmarkCategory.ANTI_SCRAPING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/anti/base64_param",
        goal="自动提取并复用经过 Base64 混淆编码的游标参数",
        expected_fields=["title", "hash"],
        min_pass_items=4,
        description="Parameter value contains obfuscated base64 token",
    ),

    # =========================================================================
    # Category 6: Captcha & Anti-Bot Detection (5 tasks)
    # =========================================================================
    BenchmarkTask(
        id="CAPTCHA-01",
        name="Cloudflare Turnstile Detection",
        category=BenchmarkCategory.CAPTCHA,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/captcha/turnstile",
        goal="自动识别页面中的 Cloudflare Turnstile 挑战并触发旁路",
        expected_fields=["challenge_type", "resolved"],
        min_pass_items=1,
        description="Detects iframe with challenges.cloudflare.com",
    ),
    BenchmarkTask(
        id="CAPTCHA-02",
        name="Geetest Slider DOM Recognition",
        category=BenchmarkCategory.CAPTCHA,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/captcha/geetest",
        goal="精准感知页面内弹出的极验滑块拼图组件",
        expected_fields=["detected_type"],
        min_pass_items=1,
        description="Recognizes .geetest_holder and slider elements",
    ),
    BenchmarkTask(
        id="CAPTCHA-03",
        name="Google reCAPTCHA Frame Sniff",
        category=BenchmarkCategory.CAPTCHA,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/captcha/recaptcha",
        goal="感知嵌入式 Google reCAPTCHA v2/v3 验证码",
        expected_fields=["detected_type"],
        min_pass_items=1,
        description="Sniffs google.com/recaptcha frames in page tree",
    ),
    BenchmarkTask(
        id="CAPTCHA-04",
        name="hCaptcha Shield Identification",
        category=BenchmarkCategory.CAPTCHA,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/captcha/hcaptcha",
        goal="自动检测并报告页面中的 hCaptcha 阻断墙",
        expected_fields=["detected_type"],
        min_pass_items=1,
        description="Detects hcaptcha.com widget",
    ),
    BenchmarkTask(
        id="CAPTCHA-05",
        name="Generic 403 Security Check Barrier",
        category=BenchmarkCategory.CAPTCHA,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/captcha/security_check",
        goal="检测页面提示'请完成安全验证'或'Security Check'阻断特征",
        expected_fields=["detected_type"],
        min_pass_items=1,
        description="Generic anti-bot challenge page detection",
    ),

    # =========================================================================
    # Category 7: Industrial Scaffolding & Formats (5 tasks)
    # =========================================================================
    BenchmarkTask(
        id="SCAFFOLD-01",
        name="HTTPX Standalone Script Generation",
        category=BenchmarkCategory.SCAFFOLDING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/scaffold/httpx",
        goal="生成轻量单文件异步爬虫并在沙箱中通过执行验证",
        expected_fields=["title", "price"],
        min_pass_items=3,
        description="Generates standalone httpx script with exit code 0",
    ),
    BenchmarkTask(
        id="SCAFFOLD-02",
        name="Scrapy 6-Piece Industrial Project",
        category=BenchmarkCategory.SCAFFOLDING,
        difficulty=DifficultyLevel.HARD,
        endpoint_path="mock/scaffold/scrapy",
        goal="导出包含 scrapy.cfg、settings、items、middlewares、pipelines 的完整 Scrapy 工程",
        expected_fields=["files_count", "ast_valid"],
        min_pass_items=1,
        description="Exports full Scrapy scaffolding with 100% valid AST",
    ),
    BenchmarkTask(
        id="SCAFFOLD-03",
        name="DrissionPage Anti-Detection Script",
        category=BenchmarkCategory.SCAFFOLDING,
        difficulty=DifficultyLevel.MEDIUM,
        endpoint_path="mock/scaffold/drission",
        goal="合成针对高风控站点的 DrissionPage Session 爬虫代码",
        expected_fields=["syntax_valid"],
        min_pass_items=1,
        description="Generates DrissionPage SessionPage crawler",
    ),
    BenchmarkTask(
        id="SCAFFOLD-04",
        name="CSV UTF-8 BOM Windows Excel Compliance",
        category=BenchmarkCategory.SCAFFOLDING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/scaffold/csv_bom",
        goal="验证导出的 CSV 文件包含标准 UTF-8 BOM 头，确保 Excel 打开不乱码",
        expected_fields=["bom_present"],
        min_pass_items=1,
        description="Exports UTF-8 BOM encoded CSV data",
    ),
    BenchmarkTask(
        id="SCAFFOLD-05",
        name="JSONL Stream Large Dataset Export",
        category=BenchmarkCategory.SCAFFOLDING,
        difficulty=DifficultyLevel.EASY,
        endpoint_path="mock/scaffold/jsonl",
        goal="导出逐行 JSONL 格式数据流并验证每行 JSON 合法性",
        expected_fields=["lines_count"],
        min_pass_items=1,
        description="Exports newline-delimited JSON data",
    ),
]


def get_task_by_id(task_id: str) -> BenchmarkTask:
    for t in SCRAPEEVAL_50_TASKS:
        if t.id == task_id:
            return t
    raise KeyError(f"Task with id '{task_id}' not found in ScrapeEval-50 dataset.")


def get_tasks_by_category(category: BenchmarkCategory) -> List[BenchmarkTask]:
    return [t for t in SCRAPEEVAL_50_TASKS if t.category == category]
